#include <stdio.h>
#include "main.h"
/**
 * main - main entry point
 *
 * if - if to repect the alphbate
 *
 * Return: Returnto zero
 */
int main(void)
{
	void print_alphabet(void);
	{
		int n;

		for (n = 'a'; n <= 'z'; n++)
		{
			_putchar(n);
		}
		_putchar('\n');
	}
	return (0);
}
