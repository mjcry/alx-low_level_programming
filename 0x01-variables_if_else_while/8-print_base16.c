#include <stdio.h>
/**
 * main - entry main funtions
 *
 * Return: Return to zero (success)
 */
int main(void)
{
	int hex = '0';
	int hex1 = 'a';

	while (hex <= '9')
	{
		putchar(hex);
		hex++;
	}

	while (b <= 'f')
	{
		putchar(hex1);
		hex1++
	}
	putchar(10);

	return (0);
}
