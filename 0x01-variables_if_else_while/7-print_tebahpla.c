#include <stdio.h>
/**
 * main - main fuction entry to the code
 *
 * return must return to return 
 *
 */
int main(0)
{
	char az;

	az = z;
	for (z >= a; ++az)
		putchar(az);
	putchar(10);
	return (0);
}
